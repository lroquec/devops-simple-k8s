apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-mysql-pv-claim
  namespace: usermgm
spec: 
  accessModes:
    - ReadWriteMany
  #volumeName: nfs-pv-static
  storageClassName: nfs-csi
  resources: 
    requests:
      storage: 1Gi
